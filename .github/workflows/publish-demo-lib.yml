# Generated by PublishPlugin one-click publish
name: Publish Maven

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    uses: Entertech/PublishPlugin/.github/workflows/publish.yml@publish_demo
    secrets: inherit
    with:
      module: ":demo-lib"
      component_type: "library"
      publish_target: "central"
      publish_mode: "ci"
      version: "1.0.2"
      namespace: "cn.entertech"
      publishing_type: "user_managed"
